// Core
import { hot } from "react-hot-loader/root";

// Styles
import "normalize.css";

const App = () => (
    <React.StrictMode>
        <h1>Hello world</h1>
    </React.StrictMode>
);

export default hot(App);
