import styled from "styled-components";

export const LogoTitle = styled.span`
    font-size: 20px;
    font-family: var(--main-font-family);
    color: white;
    text-shadow: 1px 1px 4px rgba(var(--shadow-accent), 1);
    margin-right: 7px;
`;

export const Logo = styled.div`
    a {
        display: flex;
        width: max-content;
    }
`;
