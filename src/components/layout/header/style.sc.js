import styled from "styled-components";

export const Header = styled.header`
    background-color: rgb(var(--theme-primary));
    padding: 10px;
    z-index: var(--z-index-extreme);
    display: flex;
    justify-content: space-between;
    /* position: fixed; */
`;
